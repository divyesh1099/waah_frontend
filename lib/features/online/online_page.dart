import 'package:flutter/material.dart';

class OnlinePage extends StatelessWidget {
  const OnlinePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Incoming online orders feedâ€¦'));
  }
}
